services:
  api:
    build: .
    expose:
      - "8000"
    env_file:
      - .env
    volumes:
      - ./models:/app/models
    networks:
      - updown-net
    restart: unless-stopped

networks:
  updown-net:
    external: true